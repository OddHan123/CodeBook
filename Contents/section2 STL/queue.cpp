#include<bits/stdc++.h>
using namespace std;

int main(){
//first-in first-out	
	queue<int> q;	
//把元素 x 加進 queue	
	q.push(x); 
	
//讀取排隊的最前方	
	q.front();
		
//移除最先加入的值
	q.pop();  
	
//queue內有多少元素,0代表空	
	q.size();	 
	
//true代表queue是空的	
	q.empty();  
	return 0;
}