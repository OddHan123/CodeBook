#include<bits/stdc++.h>
using namespace std;

int main(){
//由小到大排序的不重複集合
	set<int> st;

//把x加入set
	st.insert(x);

//檢查x是否在set中
	st.count(x);

//刪除x(傳入值或iterator)
	st.erase(x);

//清空set
	st.clear();

//set的大小
	st.size();
	return 0;
}